CREATE TABLE Quests (
    tehtavaID int NOT NULL,
    tehtavaNimi varchar(255),
    tehtavaKuvaus varchar(255),
    palkkioMaara int NOT NULL,
    kokemusPisteet int NOT NULL,
    onkoSuoritettu bit NOT NULL,
    CONSTRAINT PK_Quests PRIMARY KEY (tehtavaID)
);
